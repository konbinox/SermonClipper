# normal_mode.py - 讲道模式
def extract_normal(transcript, target_sec=600, log_callback=None):
    """讲道模式：基于主题匹配"""
    if log_callback:
        log_callback("\n📖 讲道模式提取...")
    
    if not transcript:
        return []
    
    # 这里放你原有的讲道模式逻辑
    # 暂时返回空，后续完善
    
    if log_callback:
        log_callback(f"   讲道模式待完善")
    
    return []